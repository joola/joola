store:
  config:
    enabled: true
    redis:
      enabled: true
      dsn: redis://127.0.0.1
  runtime:
    redis:
      enabled: true
      dsn: redis://127.0.0.1
  dispatch:
    enabled: true
    redis:
      enabled: true
      dsn: redis://127.0.0.1
    stomp:
      enabled: true
      dsn: stomp://guest:guest@127.0.0.1:61613
  websocket:
    http:
      redis:
        enabled: true
        dsn: redis://127.0.0.1
    https:
      redis:
        enabled: true
        dsn: redis://127.0.0.1
  datastore:
    mongodb:
      enabled: true
      dsn: mongodb://localhost:27017/cache
dispatch:
  enabled: true
